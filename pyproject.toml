[project]
name = "salao-girassol"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "psycopg",
    "pydantic",
    "email-validator"
]

[tool.vercel]
entrypoint = "api:app"